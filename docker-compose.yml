version: "3.8"
services:
  scc-debug:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - PORT=80
      - GIN_MODE=release
      - LOG_LEVEL=info
      - DIST_DIR=dist
      - CONFIGS_DIR=public/configs
